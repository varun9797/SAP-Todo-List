// This test ensures all source files are loaded for coverage
import './test-coverage-helper';

describe('Coverage Helper', () => {
  it('should load all source files for coverage tracking', () => {
    expect(true).toBe(true);
  });
});
